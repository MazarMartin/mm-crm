# Mazar Martin CRM Pipeline — Python dependencies
# Tested with Python 3.11+

# Browser-based scrapers (Domain, OnTheHouse, agency websites)
playwright>=1.40.0

# Patchright: maintained Playwright fork with better anti-bot stealth,
# specifically designed to defeat Akamai/Cloudflare detection. Used by
# scrape_domain_realestate.py which is aggressively blocked by Domain's
# Akamai edge. Falls back to plain playwright if this import fails.
patchright>=1.40.0

# HTML scraping (agency websites — scrape_agency_websites.py)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Excel/spreadsheet export (auxiliary lns_build_*.py scripts)
openpyxl>=3.1.0

# Note: After installing, run `playwright install chromium`
# to download the browser binary (~150 MB).
