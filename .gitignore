.DS_Store
*.bak
*.bak_*
backups/
pythonfiles/
data/
clientfiles/

# Python
.env
.venv/
venv/
__pycache__/
*.pyc

# Pipeline runtime artifacts (regenerated by scripts; not version-controlled)
pipeline/*.json
pipeline/*.log
pipeline/mazar_martin_app.html
pipeline/scripts/*.json
pipeline/scripts/*.log
pipeline/scripts/backups/
pipeline/.mm_credentials
pipeline/scripts/.mm_credentials

# Local working notes (private to David's machine)
ENGAGEMENT_NOTES.md

# Cloudflare Pages build output (assembled by build.sh in the build container)
_site/
